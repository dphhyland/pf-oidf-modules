# ── Build artifacts ──
harness/out/
**/__pycache__/
.DS_Store

# ── Secrets / licenses (never commit) ──
*.lic
*.p12
*.jks
*.key
*.pem

# ── Local Claude Code working dir (settings, launch config, skills) ──
.claude/
